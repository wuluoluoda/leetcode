class Solution(object):
    def longestCommonPrefix(self, strs):
        if len(strs) == 0 :
            return ""
        
        #遍历每列第i个字符串
        for i in range(len(strs[0])):
            
        
        return strs[0]